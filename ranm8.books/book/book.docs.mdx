---
description: Book model
labels: ['book', 'model']
---

import { Book } from './book';

Book is an in-memory represnetation of a Book.

```tsx live
() => {
  const myBook = new Book('My book');
  return <div>{myBook.name}</div>;
};
```
